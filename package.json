{
  "name": "basic-computer-games",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "elm-live src/Main.elm --pushstate --dir dist --start-page index.html -- --debug --output dist/elm.js",
    "review": "elm-review --fix",
    "build": "elm make src/Main.elm --optimize --output dist/elm.js && node scripts/build.js"
  },
  "author": "Wolfgang Schuster",
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-live": "^4.0.2",
    "elm-review": "^2.5.5",
    "esbuild": "^0.12.28"
  }
}
